//  ContentView.swift
//  ChatPrototype

import SwiftUI

struct ContentView: View {
    var body: some View {
        
        VStack {
            Text("by Mahsa Pirani")
                .font(.subheadline)
                .padding()
                .background(Color.blue, in: RoundedRectangle(cornerRadius: 8))
            
            
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
